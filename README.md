# Assignment-5

LOGIC USED:-


Task 1:

1.A dictionary stores student names as keys and marks as values
2.User input is compared with dictionary keys
3.if name in dictionary checks availability
4.If found, marks are printed
5.Otherwise, an error message is shown


Task 2:

1.range(1, 11) generates numbers from 1 to 10
2.list() converts the range into a list
3.[:5] extracts the first five elements of the list
4.[::-1] reverses the list
5.print() displays the output

